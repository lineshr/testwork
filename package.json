{
  "name": "idporten-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codeceptjs": "codeceptjs run --steps  --reporter mocha-multi",
    "codeceptjs:headless": "HEADLESS=true codeceptjs run --steps --verbose --plugins allure",
    "codeceptjs:ui": "codecept-ui --app"
  },
  "dependencies": {
    "codeceptjs": "^3.0.6",
    "playwright": "^1.10.0"
  }
}
